import { DescriptionSizePipe } from './description-size.pipe';

describe('DescriptionSizePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSizePipe();
    expect(pipe).toBeTruthy();
  });
});
